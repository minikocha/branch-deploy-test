name: main-branch-apply

on:
  issue_comment:
    types:
      - created

jobs:
  production-apply:
    if: github.event.issue.pull_request
    uses: minikocha/branch-deploy-test/.github/workflows/terragrunt-apply.yaml@main
    with:
      DEPLOYMENT_TARGET: environments/production
